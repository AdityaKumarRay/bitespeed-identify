import "dotenv/config";
import { env } from "./config/env";

/* ── Placeholder – Express app will be wired in Commit 3 ── */
console.log(`[bitespeed] Starting in ${env.NODE_ENV} mode on port ${env.PORT}`);
